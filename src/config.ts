// Just in case, let's keep the user agent as-is for now
export const typeLeapUserAgent = "FrogFind/1.0";

export const proxyDownloadMaxBytes = 8_000_000;

export const htmlDownloadMaxBytes = 4_000_000;

export const imageDownloadMaxBytes = 8_000_000;

export const fetchTimeoutMs = 10_000;

export const memoryCacheMaxBytes = 512 * 1024 * 1024;

export const compatibleContentTypes = ["text/html", "text/plain"];

export const allowedImageExtensions = [".jpg", ".jpeg", ".png"];
